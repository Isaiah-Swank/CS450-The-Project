#ifndef MERGESORT_H
#define MERGESORT_H

#include <stddef.h> // For size_t
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void merge_sort(int* arr, size_t start, size_t end, int* target);

#endif
